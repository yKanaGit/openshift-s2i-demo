apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openshift-s2i-demo
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: https://github.com/yKanaGit/openshift-s2i-demo.git
    targetRevision: main
    path: manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-s2i-demo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
